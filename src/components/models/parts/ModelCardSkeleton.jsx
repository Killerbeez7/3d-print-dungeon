export function ModelCardSkeleton() {
    return (
        <div
            className="relative bg-bg-surface rounded-md overflow-hidden animate-pulse
                      w-full aspect-square"
        >
            {/* add inner divs for title bars, avatars, etc. */}
        </div>
    );
}
